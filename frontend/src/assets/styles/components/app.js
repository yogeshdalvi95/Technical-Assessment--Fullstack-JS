import { container, mobileContainer } from "../mainStyles.js";

const appStyle = theme => ({
  content: {
    marginTop: "5rem",
    padding: "8px 9px"
  },
  container,
  mobileContainer
});

export default appStyle;
