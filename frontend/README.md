create a.env file at the root of frontend with the below contents

REACT_APP_SERVER_URL = "http://BACKEND_HOST:BACKEND_PORT"

for eg :- REACT_APP_SERVER_URL = "http://localhost:8000"

Follow steps in frontend folder
npm install
npm run start
